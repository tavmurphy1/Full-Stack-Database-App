<!-- index.hbs -->
<h1>The server is running!</h1>
<p>If you see this text, the server is <strong>definitely</strong> working! Good job Pat and Tav!</p>

{{!-- Create a table --}}
<table id="movies-table">

    {{!-- Header section --}}
    <thead>

        {{!-- For just the first row, we print each key of the row object as a header cell so we
        know what each column means when the page renders --}}
        <tr>
            {{#each data.[0]}}
            <th>
                {{@key}}
            </th>
            {{/each}}
        </tr>
    </thead>

    {{!-- Body section --}}
    <tbody>

        {{!-- For each row, print the id, fname, lname, homeworld and age, in order --}}
        {{#each data}}
        <tr>
            <td>{{this.movie_id}}</td>
            <td>{{this.movie_title}}</td>
            <td>{{this.movie_length}}</td>
            <td>{{this.movie_total_view}}</td>
        </tr>
        {{/each}}
    </tbody>
</table>

<!-- /views/index.hbs -->

{{!-- Form to add a record to the table  --}}
<h2>Adding Data with AJAX</h2>
<p>To add a new movie, please enter the information below and click 'Submit'!</p>
<form id="add-movie-form-ajax">
    <label for="input-movie_id">Movie ID: </label>
    <input type="number" name="input-movie_id" id="input-movie_id">
    
    <label for="input-movie_title">Movie Title: </label>
    <input type="text" name="input-movie_title" id="input-movie_title">

    <label for="input-movie_length">Movie Length: </label>
    <input type="number" name="input-movie_length" id="input-movie_length">

    <label for="input-age">Movie Total Views: </label>
    <input type="number" name="input-movie_total_views" id="input-movie_total_views">

    <input type="submit">
</form>

{{!-- Embed our javascript to handle the DOM manipulation and AJAX request --}}
<script src="./js/add_movie.js"></script>